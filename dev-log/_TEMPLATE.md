# [SCOPE] Краткое описание

> **Дата:** YYYY-MM-DD HH:MM
> **Агент:** Codex / Claude Code / Человек
> **Коммит:** `<hash>` (заполнить после коммита)
> **Ветка:** `feat/...` или `fix/...`

---

## Что сделано

Краткое описание изменений в 2–5 предложениях. Что добавлено, изменено или исправлено. Зачем.

## Затронутые файлы

```
packages/api/src/routes/webhooks/registration.ts    — создан
packages/api/src/services/registration.service.ts   — создан
packages/api/src/services/contact.service.ts        — изменён (добавлен метод findOrCreate)
packages/api/src/__tests__/webhooks/registration.test.ts — создан
prisma/schema.prisma                                 — изменён (добавлена модель Registration)
prisma/migrations/20260311_add_registrations/        — создана
```

## Добавленные зависимости

```
Нет новых зависимостей
— или —
packages/api: date-fns@3.x (форматирование дат)
packages/web: @tanstack/react-query@5.x (кеширование API-запросов)
```

## Связи и зависимости между компонентами

Опиши, что от чего зависит и что может сломаться при дальнейших изменениях:

- `registration.service.ts` зависит от `contact.service.findOrCreate()` — если менять логику матчинга, сломается создание регистраций
- `registration.service.ts` зависит от `webinar.service.findByRegWindow()` — если менять окна регистрации, нужно обновить и этот сервис
- Роут `/api/webhooks/registration` зависит от middleware `api-key.ts` — без ключа в заголовке вернёт 401
- Фронтенд-хук `useWebinars` вызывает `/api/webinars` — если менять формат ответа, обновить хук

## Тесты

| Тест | Статус | Что проверяет |
|------|--------|--------------|
| `registration.test.ts: создаёт регистрацию` | ✅ pass | Happy path — регистрация создаётся и привязывается к вебинару |
| `registration.test.ts: идемпотентность` | ✅ pass | Повторный вебхук не создаёт дубликат |
| `registration.test.ts: без API ключа` | ✅ pass | Возвращает 401 |

**Общий результат:** X passing, 0 failing

## Известные ограничения

- [ ] Не обработан кейс когда `gc_user_id` и `email` оба пустые — нужна валидация на уровне роута
- [ ] Окно регистрации захардкожено в тесте — нужно вынести в фикстуру
- [ ] TODO: добавить тест на создание нового вебинара при первой регистрации

## Заметки для следующего агента

Любая дополнительная информация, которая поможет следующему агенту продолжить работу. Например: нюансы реализации, неочевидные решения, планы.
